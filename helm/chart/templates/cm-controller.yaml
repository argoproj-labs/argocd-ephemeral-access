{{- with .Values.controller.config }}
apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    app.kubernetes.io/component: controller
    {{- include "chart.labels" $ | nindent 4 }}
  name: controller-cm
data:
  {{- toYaml . | nindent 2 }}
{{- end }}
