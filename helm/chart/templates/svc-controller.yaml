apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/component: controller
    {{- include "chart.labels" . | nindent 4 }}
  name: controller-metrics
spec:
  ports:
  - name: metrics
    port: 8090
    protocol: TCP
    targetPort: 8090
  selector:
    app.kubernetes.io/component: controller
