{{- with .Values.backend.config }}
apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    app.kubernetes.io/component: backend
    {{- include "chart.labels" $ | nindent 4 }}
  name: backend-cm
data:
  {{- toYaml . | nindent 2 }}
{{- end }}
